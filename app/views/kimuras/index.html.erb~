<h1>Listing kimuras</h1>

<%= paginate @kimuras %>

<table>
  <tr>
    <th>Seite</th>
    <th>Spalte</th>
    <th>Lemma</th>
    <th>Lesung</th>
    <th>Bearbeitet</th>
    <th>Kandidaten vorhanden</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @kimuras.each do |kimura| %>
  <tr>
    <td><%= kimura.kimura_page %></td>
    <td><%= kimura.kimura_column %></td>
    <td><%= kimura.kimura_lemma %></td>
    <td><%= kimura.kimura_reading %></td>
    <td><font color="#0000FF"><%= kimura.kimura_isEdited %></font></td>
    <td><%= kimura.kimura_hasCandidates %></td>
    <td><%= link_to 'Show', kimura %></td>
    <td><%= link_to 'Edit', edit_kimura_path(kimura) %></td>
    <td><%= link_to 'candidate-free ones', checkCands_path %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add a new Kimura', new_kimura_path %>
